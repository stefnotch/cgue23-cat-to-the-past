use std::sync::Arc;

use vulkano::descriptor_set::{PersistentDescriptorSet, WriteDescriptorSet};

/// references a shader and its inputs
pub struct Material {
    // TODO: Add a shader/pipeline here
    // cached_descriptor_set: Option<Arc<PersistentDescriptorSet>>,
}
